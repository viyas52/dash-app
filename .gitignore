node_modules/
.env
*.log
push_log.txt
push.bat
.DS_Store
Thumbs.db
# Dev-only preview file — never push to prod
index-dev.html
# One-off function helper/maintenance scripts (analytics, migration) — never deploy/commit
functions/_*.js
# One-off admin/maintenance probes + data snapshots (contain user financial data) — never commit
_admin_*.cjs
viyas-cloud-snapshot-*.json
_snapshot-*.json
